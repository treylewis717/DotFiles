#!/usr/bin/env bash

rustup update
doom sync
doom upgrade
read -p "Would you like to update XMonad: [y/N] " updatexmonad

if [[ $updatexmonad == "y" || $updatexmonad == "Y" || $updatexmonad == "yes" || $updatexmonad == "Yes" ]]
then
    ~/.xmonad/update.sh
fi
paru -Syu
paru -Rns $(paru -Qtdq)
